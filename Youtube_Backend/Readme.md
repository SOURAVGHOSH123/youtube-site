"A practice project by chai aur code help to improve the backend processin real time Project."

# the project is an youtube like project, is start by
# "dev": "node index.js",    // npm run dev
# "start": "nodemon index.js"  // npm run start